# vFinal Production Hardening Log

This public-safe log records the latest hardening pass toward production use.

## What Changed

- Operator Control Tower bridge health now has a dedicated schema:
  `schemas/operator-control-tower-bridge-health.schema.json`.
- The production Control Tower harness rejects weak bridge-health input. A
  generic `result: PASS` JSON is not enough.
- Parallel lanes and worktrees now have a machine-checkable contract:
  `schemas/parallel-lane-contract.schema.json`.
- The lane template is `templates/parallel-lane-contract.json`.
- `scripts/factoryctl.py validate-lane` validates scope, changed paths,
  validation result, cleanup status, evidence refs and public-safety boundary.
- The Factory 10 Whimsical map doc is now marked as historical, and the vFinal
  mind-map source is the current map source.
- A new Whimsical vFinal journey map was created in the private visual
  workspace as `external:whimsical-vfinal-journey-map`.
- Operator Control Tower now has a public-safe private evidence kit:
  `docs/control-tower/operator-control-tower-private-evidence-kit.md`.
- Bridge health now has a public-safe template:
  `templates/operator-control-tower-bridge-health.json`.
- The Operator Control Tower proof was rerun without private Discord/runtime
  evidence and correctly remained `BLOCKED`.
- Exact-tree public-safety scanning is now tracked separately in
  `docs/roadmap/release-public-safety-tree-gap.md`, because a dirty worktree can
  pass while the committed or remote release tree still fails.
- `scripts/public_safety_scan.py` now supports `--summary-json` for public-safe
  scan summaries that omit raw matching lines and raw private markers.
- Public-safe summaries now record the current state:
  `validation/public-safety/worktree-summary.json` passes, while
  `validation/public-safety/head-summary.json` and
  `validation/public-safety/origin-main-summary.json` fail until the cleaned
  tree is integrated into the release ref.
- The Operator Control Tower harness now rejects weak PASS attempts where the
  mapping lacks a real board ref, the runtime event belongs to a different
  project, the approval event id is placeholder, bridge health lacks real
  evidence refs, private evidence is passed from inside the public repo, or an
  old production proof remains after a later blocked run.
- The real-runtime update preflight now reopens the Operator Control Tower
  readiness receipt referenced by the proof and requires that receipt to be
  `PASS` with no blocking items.
- The current Operator Control Tower receipt was regenerated with the stronger
  checks and remains correctly `BLOCKED`; no stale
  `validation/hermes-production-proof/operator-control-tower.json` exists.
- The real-runtime production preflight was regenerated after the hardening and
  still blocks only `operator_control_tower` and `complete_update_receipt`.
- A private-evidence doctor now exists for the Operator Control Tower:
  `scripts/operator_control_tower_private_evidence_doctor.py`.
- A private-evidence bundle initializer now exists:
  `scripts/operator_control_tower_private_evidence_init.py`.
- The doctor writes a public-safe readiness report at
  `validation/control-tower/operator-control-tower-private-evidence-doctor.json`
  and currently blocks because the private bundle still contains placeholder
  mapping/runtime evidence and bridge health has not passed.
- Worktree release hygiene now has a public-safe inventory:
  `scripts/worktree_release_inventory.py`.
- The current inventory is
  `validation/release/worktree-release-inventory.json`. It reports 283 changed
  entries, all classified as release-candidate material, with no safe cleanup
  candidates and no unknown entries.
- Factory production readiness now has a single aggregate receipt:
  `validation/factory-production-readiness/current-readiness.json`, generated by
  `scripts/factory_production_readiness.py`.
- The aggregate receipt is currently `BLOCKED` and names the exact remaining
  blockers: Operator Control Tower proof, complete update receipt and release
  ref public-safety integration.
- Release integration now has its own public-safe preflight:
  `validation/release/release-integration-preflight.json`, generated by
  `scripts/release_integration_preflight.py`.
- The release integration preflight currently blocks because release-candidate
  material is still unintegrated, the current dirty branch is `main`, and the
  release refs still fail exact public-safety scans.
- The release integration preflight now includes a `release_candidate_plan`
  field so the next safe branch/commit step is explicit without pretending that
  production readiness has passed.

## Current Production State

Already proven:

- non-stub worker execution;
- real terminal tool auth;
- scanner-backed specialist output quality;
- parent `done` reconciliation with a real worker result;
- production rollback and monitoring recovery.

Still blocked:

- real Operator Control Tower proof;
- complete update receipt.

The Control Tower proof must still receive private evidence for:

- real Discord or equivalent cockpit mapping;
- runtime registration event;
- bridge-health receipt following
  `schemas/operator-control-tower-bridge-health.schema.json`.

Run the private-evidence doctor before the production proof:

```bash
python scripts/operator_control_tower_private_evidence_init.py \
  --out-dir /private/path/to/operator-control-tower-evidence

python scripts/operator_control_tower_private_evidence_doctor.py \
  --mapping /private/path/to/discord-control-tower-mapping.json \
  --runtime-registration-event /private/path/to/runtime-approval-event.json \
  --bridge-health /private/path/to/bridge-health.json
```

The private evidence package must use:

```text
docs/control-tower/operator-control-tower-private-evidence-kit.md
```

The complete update receipt can become `PASS` only after the Control Tower
proof passes.

The current one-file production status is:

```text
validation/factory-production-readiness/current-readiness.json
```

## Cleanup Finding

Do not run broad cleanup on this repo yet.

The current worktree contains many untracked vFinal artifacts that look like
real product changes: schemas, templates, docs, tests, Hermes adapter files,
patches and validation receipts. Cache folders are safe cleanup candidates, but
the vFinal corpus needs inventory and classification before deletion.

The current inventory is now machine-readable:

```text
validation/release/worktree-release-inventory.json
```

It deliberately omits raw file paths and records only public-safe counts. The
latest result is `ATTENTION`, not `BLOCKED`: the tree still needs release
integration, but the changes classify as release-candidate material rather than
cleanup trash.

The inventory also separates `generated_receipt` entries from real release
candidate changes. This avoids a circular proof problem where the scripts dirty
the worktree by refreshing validation receipts and then mistake those generated
receipts for unintegrated product work. Generated receipts are counted for
transparency, but they do not by themselves make the release ref unintegrated.

Before publishing, validate the exact release tree or target commit, not only
the dirty local worktree.

The public-safety scanner supports this directly:

```bash
python scripts/public_safety_scan.py --git-ref HEAD
python scripts/public_safety_scan.py --git-ref origin/main
```

## Verification Run

The following checks passed in this hardening pass:

```bash
python scripts/factoryctl.py validate-lane templates/parallel-lane-contract.json
python scripts/validate_public_json_artifacts.py
python scripts/public_safety_scan.py
python scripts/public_safety_scan.py --summary-json validation/public-safety/worktree-summary.json
python scripts/public_safety_scan.py --git-ref HEAD --summary-json validation/public-safety/head-summary.json
python scripts/public_safety_scan.py --git-ref origin/main --summary-json validation/public-safety/origin-main-summary.json
python scripts/secret_safety_scan.py
python adapters/hermes/compatibility-check.py
python -m unittest tests.test_operator_control_tower_proof -q
python -m unittest tests.test_factoryctl -q
python -m unittest discover -s tests -p "test_*.py" -q
git diff --check
```

`python scripts/operator_control_tower_proof.py` was also run without private
Control Tower evidence and correctly returned `BLOCKED`.

Additional checks after adding the private evidence kit and bridge-health
template:

```bash
python scripts/validate_public_json_artifacts.py
python scripts/public_safety_scan.py
python scripts/secret_safety_scan.py
python -m unittest tests.test_factory_production_readiness -q
python -m unittest tests.test_operator_control_tower_proof -q
python -m unittest tests.test_operator_control_tower_private_evidence_init -q
python -m unittest tests.test_operator_control_tower_private_evidence_doctor -q
python -m unittest tests.test_release_integration_preflight -q
python -m unittest tests.test_worktree_release_inventory -q
python -m unittest tests.test_release_integration_preflight tests.test_worktree_release_inventory -q
python -m unittest tests.test_hermes_production_update_preflight -q
python -m unittest discover -s tests -p "test_*.py" -q
```
