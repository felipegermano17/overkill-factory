# supply-chain-gate

Result basis:

No package install or dependency lock change was introduced; Node built-ins and local scripts are used.

Card: `DRP-FULL-LINE-PILOT`.
Authority: local validation and read-only devnet only.
