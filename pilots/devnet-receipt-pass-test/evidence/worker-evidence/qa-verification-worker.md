# qa-verification-worker

Result basis:

Devnet read, Product Face proof, scans, worker profile validation and tests passed.

Card: `DRP-FULL-LINE-PILOT`.
Authority: local validation and read-only devnet only.
