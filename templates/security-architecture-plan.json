{
  "$schema": "https://overkill-factory.dev/schemas/security-architecture-plan.schema.json",
  "trust_boundaries": ["user browser to backend", "backend to provider"],
  "data_flows": ["what sensitive or important data moves where"],
  "authn_authz": ["how identity and permissions are enforced"],
  "secrets_and_keys": ["where secrets live and who may access them"],
  "logging_and_detection": ["what must be logged and alerted"],
  "threats": ["material threat and planned control"],
  "security_owner": "security-owner",
  "evidence_refs": ["security-architecture.md"]
}
